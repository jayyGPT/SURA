# Archived model snapshots

Standalone scripts and explanatory documents previously stored under `Models (Read Only)`. They remain useful for reconstructing earlier results, but reusable development now belongs under `src/sura/`.
