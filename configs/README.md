# Experiment configuration

These YAML files record intended defaults for reproducible experiments. A result is not considered verified merely because a configuration exists; the completed run must also record its code commit, random seed, metrics, and artifacts.
