# Tests

The initial suite checks normalization, tensor interfaces, masks, model output shapes, and numerical finiteness without requiring the full dataset. Experiment-level regression tests will be added once canonical saved fixtures and checkpoints exist.
