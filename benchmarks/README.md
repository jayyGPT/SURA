# Benchmarks

The only paper-facing results are under `final_protocol/`. `knn/wifi_mag_knn.py` contains the non-temporal KNN baseline used in the final comparison.

See `final_protocol/README.md` and `final_protocol/current_results/metrics.json` for the frozen protocol, exact metrics, and provenance.
