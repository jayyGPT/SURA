# Figure workflows

Paper figures should be regenerated from machine-readable outputs under `experiments/results/`, not manually edited values. Publication-ready files belong in `paper/figures/`; exploratory plots remain in ignored experiment-run directories.
