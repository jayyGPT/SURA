# Paper figures

New figures should be generated from reviewed data under `experiments/results/` and placed here. The current manuscript's four referenced images remain in the paper root temporarily so the relocation is content-preserving.

Do not manually reconstruct a CDF from summary statistics. Regenerate it from saved per-sample errors or label it clearly as schematic.
